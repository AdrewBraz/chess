Стили для доски и фигур
Создать компонент для Доски и Ячейки
Создать Класс Доски, Ячейки, Фигур, Игрока, и Цветов
Создать перечисление цветов

Для Доски внутреннее свойство ячеек в виде двумерного массива, публичный метод инициализации доски(цикл по строкам, внутри которого цикл по столбцам)

Класс ячейки состоит из ридонли свойст координат x, y и цвета самой ячейки. А также свойство фигуры(либо фигура либо null), свойство доски, availabe и ид. В конструктор принимается доска, координаты, цвет и фигура

В корневом компоненте создается состояние доски, функция рестарт( в которой создается новая копия доски и инициализируются клетки и сохраняется состояние)

В компоненте доски создается пропс с значением самой доски и функции setboard(void). Отрисовка проходом по клеткам доски внутри, возвращается React.Fragment внутри которого проход по строке клеток => Компонент ячейки

В компоненте ячейки в качестве пропса класс Ячейки, получить из перечисления цвет, внутри компонента встает фигура

У фигуры поля цвет, лого, ячейка и название( создать перечисление из названий фигур), ид. В конструктор цвет и ячейка. На ячейку добавляется фигура в качестве текущего объекта, лого null, название базовое из перечисления, ид Math.random. Метод canmove c внутренней переменной таргет в виде ячейки и сам метод движения. Создать отнаследованные фигуры. В каждой фигуре в конструкторе определить цвет и ячейку(родительскими). Логика лого по цвету

В класс доски добавить класс раставляющий ячейки, я также метод определяющий координаты ячейки(двумерный массив). Приватные методы для добавления фигур на доску

В компоненте доски создать состояние selectedcell. В компоненте ячейки пропсом передается сама ячейка, а также флаг выбрана или нет. Если выбрана добавить класс селектед. Выбрана ли ячейка определяется по равенству между координатами текущей ячейки и той, что в состоянии. При вызове функции клик устанавливается состояние, только если на ней фигура. Создать цсс класс, который бы подсказывал возможность для хода, при условии, что фигур в клетках нет. А также класс для возможной атаки. Создать метод подсветки в котором перерисовка доски методом getCopyBoard( копия доски с ячейками this.cells). Перерисовка в useeffect при изменение состояния

В классе доски метод highlightCells. Передается селектед клетка. В функции проход по всем клеткам. Меняется свойство available, если у селектед клетки canMove(target) => true.

В классе ячейка метод moveFigure, в которую передается таргет ячейка. Если есть фигура и фигура может двигаться, то удаляется фигура с текущей ячейки и ставится на таргет ячейку

В методе клик добавить проверку -если клетка выбрана и не равна той, что хотим нажать, то вызов moveFigure

В методе canMove проверка на принадлежность к цвету, является ли фигура королем. Возврат true если проходят. В Классах фигур вызывается супер для метода canMove

В классе Ячейки проверка на пустоту диагонали, вертикали, горизонтали (проверка оси движения, получить максимальную и минимальную ячейку для таргета, проверка каждой ячейки в диапозоне на наличие фигуры. Для диагонали берется модуль между таргетом и текущей координатой для обоих осей. Направление зависит от сравнения текущей координаты и таргета, в цикле направление умножается на i-й элемент)

В классе игрока свойство цвет. В главном компоненте состояния для белого/черного игрока и состояние текущего игрока. В useeffect установка игрока. В компонент Доски передается текущий игрок и функция смены хода

В компоненте доски в функции клик проверка на вражеские фигуры. При перемещении меняется игрок

В классе доски два массива с потерянными фигурами.

В классе ячейки проверка есть ли в таргете фигура

Создать компонент с потерянными фигурами